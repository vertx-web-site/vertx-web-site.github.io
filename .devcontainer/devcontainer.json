{
  "name": "Vert.x",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "mise x -- npm i && mise x -- npm run update-docs -- --latest-bugfix-versions-only",
  "postStartCommand": "mise x -- npm run dev",
  "appPort": ["3000:3000"]
}
